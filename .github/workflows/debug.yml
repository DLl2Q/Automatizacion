name: nombre para mi prueba

on: workflow_dispatch

jobs:
  primero:
    runs-on: ubuntu-latest
    steps:
      - name: Set variable
        run: echo	"::debug::Set	variable	myVariable	to	true"