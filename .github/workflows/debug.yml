name: nombre para mi prueba

on: workflow_dispatch

jobs:
  primero:
    runs-on: ubuntu-latest
    steps:
      - name: Antes variable
        run: echo $GITHUB_ENV
      - name: Set variable
        run: echo	"FOO=bar" >> $GITHUB_ENV
      - name: Despues variable
        run: echo $FOO
