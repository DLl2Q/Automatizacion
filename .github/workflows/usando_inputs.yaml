name: workflow trasladando input
on: workflow_dispatch
jobs:
  contenedor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/
        with:
          MY_NAME: "DLLACSA"