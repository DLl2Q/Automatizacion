name: primero
on: 
  workflow_dispatch
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Descargar codigo
        uses: actions/checkout@v4
      - name: Ejecutar script
        run: echo "Hola mundo"
