name: creador por mi
on: 
  workflow_dispatch
jobs:
  firstone:
    runs-on: ubuntu-latest
    steps: 
      - name: descargar repo
        uses: actions/checkout@v4
      - name: listar contenido
        run: ls 
  secondone:
    runs-on: ubuntu-latest
    needs: firstone
    steps:
      - name: ver contenido
        run: cat archivo1.txt